<?xml version="1.0" encoding="utf-8"?>
<resources>

    <style name="Theme.Matrixia.SplashScreen" parent="Theme.SplashScreen">
        <item name="android:statusBarColor">@android:color/transparent</item>
        <item name="android:navigationBarColor">@android:color/transparent</item>

        <item name="windowSplashScreenBackground">@color/black</item>
        <item name="postSplashScreenTheme">@style/Theme.Matrixia</item>
    </style>

    <style name="Theme.Matrixia" parent="android:Theme.Material.Light.NoActionBar">
        <item name="android:statusBarColor">@android:color/transparent</item>
        <item name="android:navigationBarColor">@android:color/transparent</item>
    </style>
</resources>